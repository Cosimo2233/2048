# 2048 Python GUI

一个使用 Python 和 `tkinter` 编写的 2048 小游戏，带图形界面和独立游戏逻辑。

## 运行方式

1. 创建虚拟环境：`python -m venv .venv`
2. 激活虚拟环境：`.venv\Scripts\Activate.ps1`
3. 启动游戏：`python main.py`

## 操作说明

- 方向键或 `WASD` 移动方块
- `R` 重新开始
- 目标是合成 2048
